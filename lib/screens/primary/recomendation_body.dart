import 'package:flutter/material.dart';

class RecommendationBody extends StatelessWidget {
  const RecommendationBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Center(child: Text('Recomendation'));
}
